pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'multiloader-conventions'

include('settings-plugin')
include('root-plugin')
include('core-plugin')

