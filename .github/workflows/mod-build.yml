name: Build Minecraft mod

on:
  workflow_call:
    inputs:
      common-task:
        description: Common check to run once per version, compileJava, test, or none.
        type: string
        default: compileJava
      horizontal-jars:
        description: Build loader jars together and validate one merged jar per version.
        type: boolean
        default: false
      max-parallel:
        description: Maximum concurrent build jobs.
        type: number
        default: 4
      strict-gradle-cache:
        description: Restore Gradle home only from the same build job.
        type: boolean
        default: false
      cache-diagnostics-job:
        description: Matrix job name whose restored and built Gradle cache sizes are logged.
        type: string
        default: ''
      teakit-runner:
        description: Existing TeaKit integration, gradle or wrapper, or none to omit runtime tests.
        type: string
        default: none
      teakit-loaders:
        description: Comma-separated loaders to test on the latest configured Minecraft version.
        type: string
        default: fabric

permissions:
  contents: read

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
      teakit: ${{ steps.plan.outputs.teakit }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - id: plan
        uses: iamkaf/multiloader-conventions/.github/actions/plan-mod-build@6cf9a319c65a680c713e259120946ac49ca169a8
        with:
          common-task: ${{ inputs.common-task }}
          horizontal-jars: ${{ inputs.horizontal-jars }}
          teakit-runner: ${{ inputs.teakit-runner }}
          teakit-loaders: ${{ inputs.teakit-loaders }}

  build:
    name: Build ${{ matrix.name }}
    needs: detect-changes
    if: needs.detect-changes.outputs.matrix != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: ${{ inputs.max-parallel }}
      matrix:
        include: ${{ fromJSON(needs.detect-changes.outputs.matrix) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
      - uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0
        with:
          cache-read-only: ${{ github.event_name == 'pull_request' }}
          gradle-home-cache-strict-match: ${{ inputs.strict-gradle-cache }}

      - name: Measure restored Gradle cache
        if: matrix.name == inputs.cache-diagnostics-job
        shell: bash
        run: |
          if [ -d "$HOME/.gradle/caches" ]; then
            shopt -s nullglob
            cache_paths=("$HOME"/.gradle/caches/*)
            du -sm "$HOME/.gradle/caches" "${cache_paths[@]}" | sort -nr | sed -n '1,25p'
          fi

      - name: Build and check selected projects
        shell: bash
        env:
          MC_VERSION: ${{ matrix.version }}
          LOADERS: ${{ matrix.loaders }}
          COMMON_TASK: ${{ matrix.common }}
          MERGE_JARS: ${{ matrix.merge }}
        run: |
          set -euo pipefail
          chmod +x gradlew
          tasks=()
          if [ "$COMMON_TASK" != none ]; then
            tasks+=(":common:$MC_VERSION:$COMMON_TASK")
          fi
          IFS=',' read -ra loaders <<< "$LOADERS"
          for loader in "${loaders[@]}"; do
            tasks+=(":$loader:$MC_VERSION:build")
          done
          if [ "$MERGE_JARS" = true ]; then
            tasks+=(validateHorizontalJars)
          fi
          ./gradlew \
            "-Pmultiloader.target.versions=$MC_VERSION" \
            "-Pmultiloader.target.loaders=$LOADERS" \
            "${tasks[@]}" --console=plain --max-workers=2

      - name: Measure built Gradle cache
        if: always() && matrix.name == inputs.cache-diagnostics-job
        shell: bash
        run: |
          if [ -d "$HOME/.gradle/caches" ]; then
            shopt -s nullglob
            cache_paths=("$HOME"/.gradle/caches/*)
            du -sm "$HOME/.gradle/caches" "${cache_paths[@]}" | sort -nr | sed -n '1,25p'
          fi
          if [ -d .gradle/mavenizer/repo ]; then
            du -sm .gradle/mavenizer/repo
          fi

      - name: Upload loader jars
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}-${{ matrix.name }}
          path: ${{ matrix.artifacts }}
          if-no-files-found: error

      - name: Upload merged jar
        if: matrix.merge
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}-${{ matrix.version }}-horizontal
          path: build/libs/horizontal/${{ matrix.version }}/*.jar
          if-no-files-found: error

      - name: Upload test reports
        if: always() && matrix.common == 'test'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}-${{ matrix.version }}-tests
          path: |
            common/versions/${{ matrix.version }}/build/reports/tests/
            common/versions/${{ matrix.version }}/build/test-results/
          if-no-files-found: warn

  teakit:
    name: TeaKit ${{ matrix.name }}
    needs: detect-changes
    if: needs.detect-changes.outputs.teakit != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.detect-changes.outputs.teakit) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: iamkaf/multiloader-conventions/.github/actions/run-teakit@0d32b4c2ac53a6ef3b49feadc042678e6bf60f27
        with:
          runner: ${{ inputs.teakit-runner }}
          version: ${{ matrix.version }}
          loader: ${{ matrix.loaders }}
          java: ${{ matrix.java }}

  summary:
    needs: [detect-changes, build, teakit]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every selected build
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          printf '%s\n' "$NEEDS_JSON" >> "$GITHUB_STEP_SUMMARY"
          jq -e '
            .["detect-changes"].result == "success" and
            (if .["detect-changes"].outputs.matrix == "[]"
             then .build.result == "skipped"
             else .build.result == "success" end) and
            (if .["detect-changes"].outputs.teakit == "[]"
             then .teakit.result == "skipped"
             else .teakit.result == "success" end)
          ' <<< "$NEEDS_JSON"
